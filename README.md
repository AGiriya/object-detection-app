## Object detection using YOLOv8
This service utilizes open source model yolov8s to perform object detection in images. Upload a JPG or PNG image in the UI and download the results in a structured JSON format and the image with bounding boxes.

### Tools used
- FastAPI
- Streamlit
- Docker

### Build and run the docker container
```
docker-compose up -d
```
